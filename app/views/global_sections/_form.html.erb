<%= form_with(model: global_section, local: true) do |form| %>
  <% if global_section.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(global_section.errors.count, "error") %> prohibited this global_section from being saved:</h2>

      <ul>
        <% global_section.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="field">
    <%= form.label :schedule_id %>
    <%= form.text_field :schedule_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
